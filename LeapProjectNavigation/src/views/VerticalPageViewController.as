package views
{
	import feathers.controls.List;
	
	import starling.events.Event;
	
	public class VerticalPageViewController extends List
	{
		public function VerticalPageViewController()
		{
			/*dataProvider = collection;
			layout = listLayout;
			snapToPages = true;
			scrollBarDisplayMode = List.SCROLL_BAR_DISPLAY_MODE_NONE;
			horizontalScrollPolicy = List.SCROLL_POLICY_OFF;
			verticalScrollPolicy = List.SCROLL_POLICY_ON;
			itemRendererFactory = tileListItemRendererFactory;
			addEventListener(Event.SCROLL, _scrollHandler);*/
		}
		
		private function _scrollHandler(event:Event):void
		{
			
		}
	}
}